{% extends 'base.html.twig' %}

{% block title %}BookStatus{% endblock %}

{% block body %}
    <h1>BookStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ book_status.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ book_status.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_book_status_index') }}">back to list</a>

    <a href="{{ path('app_book_status_edit', {'id': book_status.id}) }}">edit</a>

    {{ include('book_status/_delete_form.html.twig') }}
{% endblock %}
