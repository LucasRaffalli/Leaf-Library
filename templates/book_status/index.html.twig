{% extends 'base.html.twig' %}

{% block title %}BookStatus index{% endblock %}

{% block body %}
    <h1>BookStatus index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for book_status in book_statuses %}
            <tr>
                <td>{{ book_status.id }}</td>
                <td>{{ book_status.name }}</td>
                <td>
                    <a href="{{ path('app_book_status_show', {'id': book_status.id}) }}">show</a>
                    <a href="{{ path('app_book_status_edit', {'id': book_status.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_book_status_new') }}">Create new</a>
{% endblock %}
