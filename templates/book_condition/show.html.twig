{% extends 'base.html.twig' %}

{% block title %}BookCondition{% endblock %}

{% block body %}
    <h1>BookCondition</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ book_condition.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ book_condition.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_book_condition_index') }}">back to list</a>

    <a href="{{ path('app_book_condition_edit', {'id': book_condition.id}) }}">edit</a>

    {{ include('book_condition/_delete_form.html.twig') }}
{% endblock %}
